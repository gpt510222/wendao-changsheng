# 《問道長生》飛升榜設計備忘

記錄日期：2026-09-02

狀態：人物屬性頁已預留「飛升榜」欄位；目前尚無正式飛升完成狀態與線上伺服器，因此一律顯示「尚未飛升」，不得由本機存檔推算或偽造名次。

## 顯示規則

- 未飛升：顯示「尚未飛升」。
- 已飛升並完成伺服器登記：顯示「第 N 位」。
- 排名只代表玩家在當前伺服器完成首次飛升的先後順序，不因戰力、境界或後續進度改變。
- 若未來存在多個伺服器，每個伺服器各自建立獨立飛升榜，不設跨服共用序號。

## 後端資料需求

- 玩家必須有不可重複的帳號角色 ID。
- 每個角色必須隸屬一個固定伺服器 ID。
- 伺服器需保存首次飛升時間、飛升路線、伺服器內序號與角色顯示名稱。
- 飛升名次必須由伺服器在確認飛升成功的同一筆交易中原子化產生，避免兩名玩家同時取得相同名次。
- 同一角色重送請求時只能回傳原名次，不得重複占用新的序號。
- 客戶端只能讀取排名結果，不得提交或修改自己的名次。

## 建議資料欄位

- `server_id`：所屬伺服器。
- `character_id`：角色唯一識別碼。
- `ascension_rank`：該伺服器首次飛升序號。
- `ascended_at`：伺服器確認的飛升時間。
- `ascension_path`：單途、雙途或三途飛升結果。
- `display_name_snapshot`：飛升當下角色名稱，並另保留目前名稱供榜單顯示策略選擇。

## 實作時機

待正式完成飛升條件、飛升成功流程、帳號系統與分服架構後再接入。接入前應先決定伺服器合併、角色轉服、刪除角色與回檔時是否保留原飛升名次。
